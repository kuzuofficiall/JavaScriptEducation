/*         if else kolay sorular 

Kullanıcıdan bir sayı alın ve bu sayının pozitif, negatif veya sıfır olup olmadığını kontrol eden bir program yazın.


let sayi = Number(prompt("lütfen bir sayı giriniz"));

if (sayi > 0) {
    console.log("sayı pozitiftir")
} else if (sayi == 0) {
    console.log("sayı sıfıta eşitir")
} else if (sayi < 0) {
    console.log("sayı negatiftir")
}
*/

/*
                          Kullanıcı Girişi:
Kullanıcıdan bir kullanıcı adı ve şifre alıp, doğru girildiğinde "Giriş Başarılı",
 yanlış girildiğinde "Giriş Başarısız" mesajı veren bir program yazın.
 
 let username = prompt("kulanıcı adını giriniz");
 let password = prompt("şifrenizi giriniz");
 
 const confirimPassword = "1234";
 const confirimUsername = "abdullahkuzu"
 
 if (username == confirimUsername && password == confirimPassword) {
    console.log("giriş başarılı")
    
} else {
    console.log("giriş başarısız")
}
*/

/*
                             Sıcaklık Durumu:

Kullanıcıdan sıcaklık değerini alın. 
Eğer sıcaklık 30 dereceden büyükse "Sıcak", 10 dereceden küçükse "Soğuk", diğer durumlarda ise "Normal" yazdırın.



let sıcaklık = Number(prompt("sıcaklık değeri girin "));

if (sıcaklık > 30) {
    console.log("sıcaklık büyük")
} else if (sıcaklık < 10) {
    console.log("soğuk")
} else {
    console.log("normal")
}
*/

/*
                    Sayı Çift Mi Tek Mi?

Kullanıcıdan bir sayı alın ve bu sayının çift mi yoksa tek mi olduğunu kontrol eden bir program yazın.

let sayı = Number(prompt("lütfen sayı giriiniz"));

if (sayı % 2 == 0) {
    console.log("sayi çiftir")
} else {
    console.log(" sayi tektir")
}
*/